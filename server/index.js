import 'dotenv/config'; import express from 'express'; import cors from 'cors'; import { hasSupabase, supabaseAdmin } from './config/supabase.js'; import { rankTrainers } from './services/competency.js';
const app=express(); app.use(cors()); app.use(express.json());
app.get('/api/health',(req,res)=>res.json({ok:true,service:'capacity-connect-api',supabase:hasSupabase}));
app.post('/api/competency/recommendations',async(req,res)=>{const topic=req.body?.topic||{skills:['Doppler Radar','Nowcasting','Data Interpretation']};if(!hasSupabase)return res.json({model_version:'1.0-demo',results:[{trainer_id:'demo-1',name:'Dr. Ananya Rao',score:96,breakdown:{skills:.97,experience:1,qualification:.95,performance:.96,availability:.9,certifications:.85},explanation:'Recommended because this trainer matches 3 of 3 required competencies, has 11 years of relevant experience, and maintains an average learner rating of 4.8/5.'},{trainer_id:'demo-2',name:'Rajiv Menon',score:91,breakdown:{skills:.9,experience:1,qualification:.9,performance:.91,availability:.8,certifications:.8},explanation:'Recommended for deep radar operations experience and strong learner outcomes.'}]});try{const {data,error}=await supabaseAdmin.from('trainer_competency_view').select('*');if(error)throw error;res.json({model_version:'1.0',results:rankTrainers(topic,data||[])})}catch(e){res.status(500).json({error:e.message})}});
app.get('/api/courses',async(req,res)=>{if(!hasSupabase)return res.json({courses:[] ,demo:true});const {data,error}=await supabaseAdmin.from('courses').select('*,course_modules(*)').eq('status','PUBLISHED').order('created_at',{ascending:false});if(error)return res.status(500).json({error:error.message});res.json({courses:data})});
app.post('/api/admin/users/:id/approve',async(req,res)=>{if(!hasSupabase)return res.json({ok:true,demo:true});const {data,error}=await supabaseAdmin.from('profiles').update({status:'APPROVED'}).eq('id',req.params.id).select().single();if(error)return res.status(500).json({error:error.message});res.json({profile:data})});
app.post('/api/assessments/:id/submit',async(req,res)=>{if(!hasSupabase)return res.json({score:82,passed:true,demo:true});res.status(501).json({error:'Wire assessment submission to authenticated attempt service before production.'})});
app.listen(process.env.PORT||4000,()=>console.log(`Capacity Connect API listening on ${process.env.PORT||4000}`));
