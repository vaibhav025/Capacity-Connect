# Frontend (safe public values)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_URL=http://localhost:4000

# Backend only — never use these in VITE_* variables
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
PORT=4000
